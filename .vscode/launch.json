{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Pixi Run Streamlit App",
            "type": "node-terminal", // or "bash" if you prefer
            "request": "launch",
            "command": "pixi run streamlit run hello_world.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
