{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Pixi Run Streamlit App",
            "type": "node-terminal", // or "bash" if you prefer
            "request": "launch",
            "command": "pixi run streamlit run map_with_new_marker.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
